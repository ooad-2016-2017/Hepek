<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Parkomania"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Maps="using:Windows.UI.Xaml.Controls.Maps"
    x:Class="Parkomania.GlavnaForma"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="0*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Button x:ConnectionId='1' x:Name="button" Command="{Binding Path=exit}" Content="Exit" HorizontalAlignment="Stretch" Margin="5,0,5,0" Grid.Row="1" VerticalAlignment="Center"/>
            <Button x:ConnectionId='2' x:Name="button1" Command="{Binding Path=findme}" Content="FindMe!" HorizontalAlignment="Stretch" Margin="5,0,5,0" Grid.Row="1" VerticalAlignment="Center" Grid.Column="2"/>
            <Maps:MapControl x:ConnectionId='3' x:Name="mapa" HorizontalAlignment="Stretch" Margin="10,10,10,10" Grid.ColumnSpan="4" VerticalAlignment="Stretch" Center="{Binding TrenutnaLokacija, Mode=OneWay}"/>
            <AutoSuggestBox Text="{Binding Path=searchparkingguest, Mode=TwoWay}" Grid.Column="3" HorizontalAlignment="Stretch" Margin="10,0,10,0" Grid.Row="1" VerticalAlignment="Center" PlaceholderText="Search">
                <AutoSuggestBox VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
            </AutoSuggestBox>
        </Grid>

    </Grid>
</Page>

